const dbOptions = {
  /* don't expose password or any sensitive info, done only for demo */
  host: "localhost",
  user: "root",
  password: "andrei",
  port: 3306,
  database: "organize_people",
};

module.exports = dbOptions;
